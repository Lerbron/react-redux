/** * Created by admin on 2017/3/3. */import { Router, hashHistory} from 'react-router';import React from 'react';// import Main from 'containers/main';// import Seller from 'containers/seller';// import Mine from 'containers/mine';// import More from 'containers/more';// import Home from 'containers/home';const routers = {  path:'/',  // component : Main,	getComponent(nextState,callback){		require.ensure([],require=>{			callback(null,require('containers/main').default);		},'');	},  indexRoute:{    //  component:Home,	  getComponent(nextState,callback){		  require.ensure([],require=>{			  callback(null,require('containers/home').default);		  },'home');	  }  },  childRoutes:[    {      path:'home',	    // component:Home,	    getComponent(nextState,callback){		    require.ensure([],require=>{			    callback(null,require('containers/home').default);		    },'home');	    }    },    {      path:'seller',      // component:Seller,	    getComponent(nextState,callback){		    require.ensure([],require=>{			    callback(null,require('containers/seller').default);		    },'seller');	    }    },    {      path:'mine',      // component:Mine,	    getComponent(nextState,callback){		    require.ensure([],require=>{			    callback(null,require('containers/mine').default);		    },'mine');	    }    },    {      path:'more',      // component:More	    getComponent(nextState,callback){		    require.ensure([],require=>{			    callback(null,require('containers/more').default);		    },'more');	    }    }  ]};export default <Router routes={routers} history={hashHistory}/>