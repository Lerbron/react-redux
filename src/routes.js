/** * Created by admin on 2017/3/3. */import { Router, hashHistory} from 'react-router';import React from 'react';// import Main from 'containers/main';// import Seller from 'containers/seller';// import Mine from 'containers/mine';// import More from 'containers/more';// import Home from 'containers/home';// 按需加载const routers = {  path:'/',	getComponent(nextState,callback){		require.ensure([],require=>{			callback(null,require('containers/main').default);    // 按需加载时，require不能使用变量，且之前不能使用import将组件引入		},'');	},  indexRoute:{	  getComponent(nextState,callback){		  require.ensure([],require=>{			  callback(null,require('containers/home').default);		  },'home');	  }  },  childRoutes:[    {      path:'home',	    getComponent(nextState,callback){		    require.ensure([],require=>{			    callback(null,require('containers/home').default);		    },'home');	    }    },    {      path:'seller',	    getComponent(nextState,callback){		    require.ensure([],require=>{			    callback(null,require('containers/seller').default);		    },'seller');	    }    },    {      path:'mine',	    getComponent(nextState,callback){		    require.ensure([],require=>{			    callback(null,require('containers/mine').default);		    },'mine');	    }    },    {      path:'more',	    getComponent(nextState,callback){		    require.ensure([],require=>{			    callback(null,require('containers/more').default);		    },'more');	    }    }  ]};// const routers = {// 	path:'/',// 	component : Main,// 	indexRoute:{// 		component:Home,// 	},// 	childRoutes:[// 		{// 			path:'home',// 			component:Home,// 		},// 		{// 			path:'seller',// 			component:Seller,// 		},// 		{// 			path:'mine',// 			component:Mine,// 		},// 		{// 			path:'more',// 			component:More// 		}// 	]// };export default <Router routes={routers} history={hashHistory}/>